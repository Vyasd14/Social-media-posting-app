<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Document</title>
</head>
<body>
    
      <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
 <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>   
      </div>
       <div class="flex items-start gap-3">
           <div class="w-10 h-10 bg-red-500 rounded-md overflow-hidden">
               <img class="w-full h-full object-cover" src="/imges/uploads/<%=user.profilepic %>" alt="">
           </div>
        <h1>jai shree mahakal,<%=user.name %></h1>

       </div>
       
       <h5 class="mb-5 mt-3">you can create a new post.</h5>
       
      <form method="post" action="/post">
          <textarea placeholder="whats on your mind @" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"name="content"></textarea>
      <input class="px-3 w-40 py-2 text-sm bg-blue-500 block rounded-md mt-2" type="submit" value="Create new post">
       </form>
    <div class="post mt-20">
        <h3 >Your Posts.</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post) {%>
            <div class="post w-1/3 mb-2 rounded-md border-2 border-zinc-800"></div>
            <h4 class="text-blue-500 mb-2">@<%=user.username %></h4>
            <p class="text-sm tracking-tight"><%=post.content %>  </p>
              <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
            <div class="btns flex gap-4 mt-1">
                <a class="text-blue-500" href="/like/<%= post._id %>">  
                    <%= post.likes.indexOf(user._id)=== -1 ? "like" :"Unlike" %>
                </a>
               <a class="text-zinc-600"href="/edit/<%= post._id %>">edit</a>
            </div>    
        </div>
        <% }) %>
    </div>
</div>
</body>
</html>